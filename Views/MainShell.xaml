<Window x:Class="RimWorldTranslationTool.Views.MainShell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:RimWorldTranslationTool.Views"
        mc:Ignorable="d"
        Title="RimWorld 翻譯工具" 
        Height="800" Width="1400"
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgPrimaryBrush}"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" 
                      CornerRadius="12"
                      GlassFrameThickness="0"
                      ResizeBorderThickness="6"/>
    </WindowChrome.WindowChrome>
    
    <Border CornerRadius="12" 
            BorderBrush="{StaticResource BorderBrush}" 
            BorderThickness="1"
            Background="{StaticResource BgPrimaryBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 自訂標題列 -->
            <Border Grid.Row="0" Background="{StaticResource BgSecondaryBrush}" CornerRadius="12,12,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Logo -->
                    <StackPanel Orientation="Horizontal" Margin="16,0" VerticalAlignment="Center">
                        <TextBlock Text="🎮" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="RimWorld 翻譯工具" 
                                   FontSize="13" 
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- 視窗控制按鈕 -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="MinimizeButton" Content="─" 
                                Style="{StaticResource IconButton}" 
                                Click="MinimizeButton_Click"
                                Width="46" Height="40" FontSize="12"/>
                        <Button x:Name="MaximizeButton" Content="□" 
                                Style="{StaticResource IconButton}" 
                                Click="MaximizeButton_Click"
                                Width="46" Height="40" FontSize="12"/>
                        <Button x:Name="CloseButton" Content="✕" 
                                Click="CloseButton_Click"
                                Width="46" Height="40" FontSize="12">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource IconButton}">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- 主內容區 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- 側邊導航 -->
                <Border Background="{StaticResource SidebarGradient}" 
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="0,0,1,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- 導航選單 -->
                        <StackPanel Margin="0,16,0,0">
                            <TextBlock Text="主選單" 
                                       FontSize="11" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       Margin="24,0,0,12"/>
                            
                            <RadioButton x:Name="NavSettings" 
                                         Style="{StaticResource NavButton}"
                                         IsChecked="True"
                                         Checked="NavButton_Checked"
                                         Tag="Settings">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⚙️" FontSize="16" Margin="0,0,12,0"/>
                                    <TextBlock Text="設定" VerticalAlignment="Center"/>
                                </StackPanel>
                            </RadioButton>
                            
                            <RadioButton x:Name="NavModBrowser" 
                                         Style="{StaticResource NavButton}"
                                         Checked="NavButton_Checked"
                                         Tag="ModBrowser">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🔍" FontSize="16" Margin="0,0,12,0"/>
                                    <TextBlock Text="模組瀏覽器" VerticalAlignment="Center"/>
                                </StackPanel>
                            </RadioButton>
                            
                            <RadioButton x:Name="NavModManager" 
                                         Style="{StaticResource NavButton}"
                                         Checked="NavButton_Checked"
                                         Tag="ModManager">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📦" FontSize="16" Margin="0,0,12,0"/>
                                    <TextBlock Text="模組管理器" VerticalAlignment="Center"/>
                                </StackPanel>
                            </RadioButton>
                            
                            <Border Height="1" 
                                    Background="{StaticResource BorderBrush}" 
                                    Margin="16,16"/>
                            
                            <TextBlock Text="工具" 
                                       FontSize="11" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       Margin="24,0,0,12"/>
                            
                            <RadioButton x:Name="NavTranslation" 
                                         Style="{StaticResource NavButton}"
                                         Checked="NavButton_Checked"
                                         Tag="Translation">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🌐" FontSize="16" Margin="0,0,12,0"/>
                                    <TextBlock Text="翻譯工具" VerticalAlignment="Center"/>
                                </StackPanel>
                            </RadioButton>
                        </StackPanel>
                        
                        <!-- 底部資訊 -->
                        <Border Grid.Row="1" 
                                Background="{StaticResource BgTertiaryBrush}" 
                                CornerRadius="10"
                                Margin="12,0,12,16"
                                Padding="14">
                            <StackPanel>
                                <TextBlock Text="RimWorld 翻譯工具" 
                                           FontSize="12" 
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}"/>
                                <TextBlock Text="v1.0.0" 
                                           FontSize="11" 
                                           Foreground="{StaticResource TextMutedBrush}"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
                
                <!-- 內容區域 -->
                <Border Grid.Column="1" Background="{StaticResource BgPrimaryBrush}">
                    <ContentControl x:Name="ContentArea"/>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
<Window x:Class="RimWorldTranslationTool.Views.MainShell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:RimWorldTranslationTool.Views"
        mc:Ignorable="d"
        Title="RimWorld 翻譯工具" 
        Height="800" Width="1400"
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgPrimaryBrush}"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="44" 
                      CornerRadius="14"
                      GlassFrameThickness="0"
                      ResizeBorderThickness="6"/>
    </WindowChrome.WindowChrome>
    
     <Border CornerRadius="14" 
             BorderBrush="{StaticResource BorderBrush}" 
             BorderThickness="1"
             Background="{StaticResource BgPrimaryBrush}">
         <Grid>
             <Grid.RowDefinitions>
                 <RowDefinition Height="44"/>
                 <RowDefinition Height="*"/>
             </Grid.RowDefinitions>
             
            
             <!-- 自訂標題列 -->
             <Border Grid.Row="0" Background="{StaticResource BgSecondaryBrush}" CornerRadius="14,14,0,0">
                 <Grid>
                     <Grid.ColumnDefinitions>
                         <ColumnDefinition Width="Auto"/>
                         <ColumnDefinition Width="*"/>
                         <ColumnDefinition Width="Auto"/>
                     </Grid.ColumnDefinitions>
                    
                    
                    <!-- Logo -->
                     <StackPanel Orientation="Horizontal" Margin="16,0" VerticalAlignment="Center">
                         <Border Background="{StaticResource AccentGradient}" CornerRadius="8" Padding="8" Margin="0,0,10,0">
                             <TextBlock Text="🎮" FontSize="14" VerticalAlignment="Center"/>
                         </Border>
                         <StackPanel VerticalAlignment="Center">
                             <TextBlock Text="RimWorld 翻譯工具" 
                                        FontSize="12" 
                                        FontWeight="SemiBold"
                                        Foreground="{StaticResource TextPrimaryBrush}"
                                        VerticalAlignment="Center"/>
                             <TextBlock Text="Translation Workspace" 
                                        FontSize="10"
                                        Foreground="{StaticResource TextMutedBrush}"
                                        Margin="0,1,0,0"/>
                         </StackPanel>
                     </StackPanel>
                    
                    
                    <!-- 視窗控制按鈕 -->
                     <StackPanel Grid.Column="2" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                         <Button x:Name="MinimizeButton" Content="─" 
                                 Style="{StaticResource IconButton}" 
                                 Click="MinimizeButton_Click"
                                 Width="46" Height="44" FontSize="12"/>
                         <Button x:Name="MaximizeButton" Content="□" 
                                 Style="{StaticResource IconButton}" 
                                 Click="MaximizeButton_Click"
                                 Width="46" Height="44" FontSize="12"/>
                         <Button x:Name="CloseButton" Content="✕" 
                                 Click="CloseButton_Click"
                                 Width="46" Height="44" FontSize="12">
                             <Button.Style>
                                 <Style TargetType="Button" BasedOn="{StaticResource IconButton}">
                                     <Style.Triggers>
                                         <Trigger Property="IsMouseOver" Value="True">
                                             <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
             </Border>
             
            
             <!-- 主內容區 -->
             <Grid Grid.Row="1">
                 <Grid.ColumnDefinitions>
                     <ColumnDefinition Width="264"/>
                     <ColumnDefinition Width="*"/>
                 </Grid.ColumnDefinitions>
                 
            
                 <!-- 側邊導航 -->
                 <Border Background="{StaticResource SidebarGradient}" 
                         BorderBrush="{StaticResource BorderBrush}"
                         BorderThickness="0,0,1,0">
                     <Grid>
                         <Grid.RowDefinitions>
                             <RowDefinition Height="Auto"/>
                             <RowDefinition Height="*"/>
                             <RowDefinition Height="Auto"/>
                         </Grid.RowDefinitions>
                         
            
                         <Border Background="{StaticResource BgSecondaryBrush}" BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderBrush}" Padding="16">
                             <StackPanel>
                                 <TextBlock Text="工作區" FontSize="10" Foreground="{StaticResource TextMutedBrush}"/>
                                 <TextBlock Text="RimWorld" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4,0,0"/>
                                 <TextBlock Text="翻譯 / 模組 / 設定" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                             </StackPanel>
                         </Border>
                         
            
                         <!-- 導航選單 -->
                         <StackPanel Grid.Row="1" Margin="0,16,0,0">
                             <TextBlock Text="主選單" 
                                        FontSize="11" 
                                        FontWeight="SemiBold"
                                        Foreground="{StaticResource TextMutedBrush}"
                                       Margin="24,0,0,12"/>
                            
            
                             <RadioButton x:Name="NavSettings" 
                                          Style="{StaticResource NavButton}"
                                          IsChecked="True"
                                          Checked="NavButton_Checked"
                                          Tag="Settings">
                                 <StackPanel Orientation="Horizontal">
                                     <TextBlock Text="⚙️" FontSize="15" Margin="0,0,12,0"/>
                                     <TextBlock Text="設定" VerticalAlignment="Center"/>
                                 </StackPanel>
                             </RadioButton>
                            
            
                             <RadioButton x:Name="NavModBrowser" 
                                          Style="{StaticResource NavButton}"
                                          Checked="NavButton_Checked"
                                          Tag="ModBrowser">
                                 <StackPanel Orientation="Horizontal">
                                     <TextBlock Text="🔍" FontSize="15" Margin="0,0,12,0"/>
                                     <TextBlock Text="模組瀏覽器" VerticalAlignment="Center"/>
                                 </StackPanel>
                             </RadioButton>
                            
            
                             <RadioButton x:Name="NavModManager" 
                                          Style="{StaticResource NavButton}"
                                          Checked="NavButton_Checked"
                                          Tag="ModManager">
                                 <StackPanel Orientation="Horizontal">
                                     <TextBlock Text="📦" FontSize="15" Margin="0,0,12,0"/>
                                     <TextBlock Text="模組管理器" VerticalAlignment="Center"/>
                                 </StackPanel>
                             </RadioButton>
                            
            
                            <Border Height="1" 
                                    Background="{StaticResource BorderBrush}" 
                                    Margin="16,16"/>
                            
            
                            <TextBlock Text="工具" 
                                       FontSize="11" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       Margin="24,0,0,12"/>
                            
            
                             <RadioButton x:Name="NavTranslation" 
                                          Style="{StaticResource NavButton}"
                                          Checked="NavButton_Checked"
                                          Tag="Translation"
                                          IsEnabled="False"
                                          Opacity="0.55">
                                 <StackPanel Orientation="Horizontal">
                                     <TextBlock Text="🌐" FontSize="15" Margin="0,0,12,0"/>
                                     <StackPanel>
                                         <TextBlock Text="翻譯工具" VerticalAlignment="Center"/>
                                         <TextBlock Text="Coming soon" FontSize="10" Foreground="{StaticResource TextMutedBrush}"/>
                                     </StackPanel>
                                 </StackPanel>
                             </RadioButton>
                         </StackPanel>
                         
            
                         <!-- 底部資訊 -->
                         <Border Grid.Row="2" 
                                 Background="{StaticResource BgTertiaryBrush}" 
                                 CornerRadius="12"
                                 Margin="12,0,12,16"
                                 Padding="14">
                             <StackPanel>
                                 <TextBlock Text="RimWorld 翻譯工具" 
                                            FontSize="12" 
                                            FontWeight="SemiBold"
                                            Foreground="{StaticResource TextPrimaryBrush}"/>
                                <TextBlock Text="v1.0.0" 
                                           FontSize="11" 
                                           Foreground="{StaticResource TextMutedBrush}"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                 </Border>
                 
            
                 <!-- 內容區域 -->
                 <Grid Grid.Column="1">
                     <Grid.RowDefinitions>
                         <RowDefinition Height="Auto"/>
                         <RowDefinition Height="*"/>
                     </Grid.RowDefinitions>
                     
            
                     <Border Grid.Row="0" Background="{StaticResource BgSecondaryBrush}" BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderBrush}" Padding="16">
                         <Grid>
                             <Grid.ColumnDefinitions>
                                 <ColumnDefinition Width="*"/>
                                 <ColumnDefinition Width="360"/>
                             </Grid.ColumnDefinitions>
                             
            
                             <StackPanel>
                                 <TextBlock Text="控制台" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                 <TextBlock Text="在左側切換功能，右側查看內容" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,3,0,0"/>
                             </StackPanel>
                             
            
                             <TextBox Grid.Column="1" Style="{StaticResource ModernTextBox}" Text="" />
                         </Grid>
                     </Border>
                     
            
                     <Border Grid.Row="1" Background="{StaticResource BgPrimaryBrush}" Padding="0">
                         <ContentControl x:Name="ContentArea"/>
                     </Border>
                 </Grid>
             </Grid>
         </Grid>
     </Border>
 </Window>