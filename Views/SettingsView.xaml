<UserControl x:Class="RimWorldTranslationTool.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled"
                  Style="{StaticResource ModernScrollViewer}">
        <StackPanel Margin="32,24">
            
            <!-- 頁面標題 -->
            <TextBlock Text="設定" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,8"/>
            <TextBlock Text="配置 RimWorld 路徑和應用程式偏好設定" 
                       FontSize="14" 
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,0,0,32"/>
            
            <!-- 遊戲路徑設定卡片 -->
            <Border Style="{StaticResource Card}" Margin="0,0,0,20">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Background="{StaticResource AccentBrush}" 
                                CornerRadius="8" 
                                Padding="10" 
                                Margin="0,0,12,0">
                            <TextBlock Text="🎮" FontSize="18"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="遊戲路徑" 
                                       FontSize="16" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="設定 RimWorld 安裝目錄" 
                                       FontSize="12" 
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Style="{StaticResource ModernTextBox}"
                                 Text="{Binding GamePath, UpdateSourceTrigger=PropertyChanged}"
                                 VerticalAlignment="Center"/>
                        <Button Grid.Column="1" 
                                Content="瀏覽" 
                                Style="{StaticResource GhostButton}"
                                Command="{Binding BrowseGamePathCommand}"
                                Margin="12,0,0,0"/>
                    </Grid>
                    
                    <!-- 路徑狀態 -->
                    <Border Background="{StaticResource BgTertiaryBrush}" 
                            CornerRadius="8" 
                            Padding="14">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="✓" 
                                       FontSize="14" 
                                       Foreground="{StaticResource SuccessBrush}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,10,0"
                                       Visibility="{Binding IsGamePathValid, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding GamePathStatus}" 
                                       FontSize="13" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>
            
            <!-- ModsConfig 設定卡片 -->
            <Border Style="{StaticResource Card}" Margin="0,0,0,20">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Background="{StaticResource WarningBrush}" 
                                CornerRadius="8" 
                                Padding="10" 
                                Margin="0,0,12,0">
                            <TextBlock Text="📁" FontSize="18"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="ModsConfig.xml" 
                                       FontSize="16" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="模組配置檔案路徑" 
                                       FontSize="12" 
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Style="{StaticResource ModernTextBox}"
                                 Text="{Binding ModsConfigPath}"
                                 IsReadOnly="True"
                                 VerticalAlignment="Center"/>
                        <Button Grid.Column="1" 
                                Content="自動偵測" 
                                Style="{StaticResource GhostButton}"
                                Command="{Binding AutoDetectCommand}"
                                Margin="12,0,0,0"/>
                        <Button Grid.Column="2" 
                                Content="選擇檔案" 
                                Style="{StaticResource GhostButton}"
                                Command="{Binding SelectModsConfigCommand}"
                                Margin="12,0,0,0"/>
                    </Grid>
                </StackPanel>
            </Border>
            
            <!-- 語言設定卡片 -->
            <Border Style="{StaticResource Card}" Margin="0,0,0,20">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Background="{StaticResource InfoBrush}" 
                                CornerRadius="8" 
                                Padding="10" 
                                Margin="0,0,12,0">
                            <TextBlock Text="🌐" FontSize="18"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="語言與翻譯" 
                                       FontSize="16" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="介面語言和翻譯偏好設定" 
                                       FontSize="12" 
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- 介面語言 -->
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="介面語言" 
                                       FontSize="13" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ComboBox ItemsSource="{Binding AvailableLanguages}"
                                      SelectedItem="{Binding SelectedLanguage}"
                                      Height="44"
                                      FontSize="13"/>
                        </StackPanel>
                        
                        <!-- 遊戲版本 -->
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="遊戲版本" 
                                       FontSize="13" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ComboBox ItemsSource="{Binding AvailableVersions}"
                                      SelectedItem="{Binding SelectedVersion}"
                                      Height="44"
                                      FontSize="13"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
            
            <!-- 操作按鈕 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="重設為預設值" 
                        Style="{StaticResource GhostButton}"
                        Command="{Binding ResetSettingsCommand}"
                        Margin="0,0,12,0"/>
                <Button Content="儲存設定" 
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding SaveSettingsCommand}"/>
            </StackPanel>
            
        </StackPanel>
    </ScrollViewer>
</UserControl>
<UserControl x:Class="RimWorldTranslationTool.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled"
                  Style="{StaticResource ModernScrollViewer}">
        <StackPanel Margin="24,20">
            
            <!-- 頁面標題 -->
            <TextBlock Text="設定" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,8"/>
            <TextBlock Text="配置 RimWorld 路徑和應用程式偏好設定" 
                       FontSize="12" 
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,0,0,20"/>
            
            <!-- 遊戲路徑設定卡片 -->
            <Border Style="{StaticResource Card}" Margin="0,0,0,16">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Background="{StaticResource AccentBrush}" 
                                CornerRadius="8" 
                                Padding="10" 
                                Margin="0,0,12,0">
                            <TextBlock Text="🎮" FontSize="18"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="遊戲路徑" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="設定 RimWorld 安裝目錄" 
                                       FontSize="11" 
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Style="{StaticResource ModernTextBox}"
                                 Text="{Binding GamePath, UpdateSourceTrigger=PropertyChanged}"
                                 VerticalAlignment="Center"/>
                        <Button Grid.Column="1" 
                                Content="瀏覽" 
                                Style="{StaticResource GhostButton}"
                                Command="{Binding BrowseGamePathCommand}"
                                Margin="12,0,0,0"/>
                    </Grid>
                    
                    <!-- 路徑狀態 -->
                    <Border Background="{StaticResource BgTertiaryBrush}" 
                            CornerRadius="8" 
                            Padding="14">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="✓" 
                                       FontSize="14" 
                                       Foreground="{StaticResource SuccessBrush}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,10,0"
                                       Visibility="{Binding IsGamePathValid, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding GamePathStatus}" 
                                       FontSize="13" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>
            
            <!-- ModsConfig 設定卡片 -->
            <Border Style="{StaticResource Card}" Margin="0,0,0,16">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Background="{StaticResource WarningBrush}" 
                                CornerRadius="8" 
                                Padding="10" 
                                Margin="0,0,12,0">
                            <TextBlock Text="📁" FontSize="18"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="ModsConfig.xml" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="模組配置檔案路徑" 
                                       FontSize="11" 
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Style="{StaticResource ModernTextBox}"
                                 Text="{Binding ModsConfigPath}"
                                 IsReadOnly="True"
                                 VerticalAlignment="Center"/>
                        <Button Grid.Column="1" 
                                Content="自動偵測" 
                                Style="{StaticResource GhostButton}"
                                Command="{Binding AutoDetectCommand}"
                                Margin="12,0,0,0"/>
                        <Button Grid.Column="2" 
                                Content="選擇檔案" 
                                Style="{StaticResource GhostButton}"
                                Command="{Binding SelectModsConfigCommand}"
                                Margin="12,0,0,0"/>
                    </Grid>
                </StackPanel>
            </Border>
            
            <!-- 語言設定卡片 -->
            <Border Style="{StaticResource Card}" Margin="0,0,0,16">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Background="{StaticResource InfoBrush}" 
                                CornerRadius="8" 
                                Padding="10" 
                                Margin="0,0,12,0">
                            <TextBlock Text="🌐" FontSize="18"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="語言與翻譯" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="介面語言和翻譯偏好設定" 
                                       FontSize="11" 
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- 介面語言 -->
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="介面語言" 
                                       FontSize="13" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ComboBox ItemsSource="{Binding AvailableLanguages}"
                                      SelectedItem="{Binding SelectedLanguage}"
                                      Height="44"
                                      FontSize="13"/>
                        </StackPanel>
                        
                        <!-- 遊戲版本 -->
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="遊戲版本" 
                                       FontSize="13" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ComboBox ItemsSource="{Binding AvailableVersions}"
                                      SelectedItem="{Binding SelectedVersion}"
                                      Height="44"
                                      FontSize="13"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
            
            <!-- 操作按鈕 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="重設為預設值" 
                        Style="{StaticResource GhostButton}"
                        Command="{Binding ResetSettingsCommand}"
                        Margin="0,0,12,0"/>
                <Button Content="儲存設定" 
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding SaveSettingsCommand}"/>
            </StackPanel>
            
        </StackPanel>
    </ScrollViewer>
</UserControl>