<Window x:Class="RimWorldTranslationTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RimWorld 翻譯工具" Height="750" Width="1300"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 頂部標題區 -->
        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="24,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🎮" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="RimWorld 翻譯工具" Style="{StaticResource HeaderTextStyle}"/>
                        <TextBlock Text="管理和檢視模組翻譯狀態" Style="{StaticResource LabelTextStyle}" Margin="0"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- 快速操作區 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="遊戲版本:" VerticalAlignment="Center" Margin="0,0,8,0" 
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                    <ComboBox x:Name="GameVersionComboBox" Width="80" Height="32" 
                              Style="{StaticResource ModernComboBoxStyle}"
                              SelectionChanged="GameVersionComboBox_SelectionChanged"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 主要內容區 - TabControl -->
        <TabControl Grid.Row="1" Margin="24,16,24,0">
            
            <!-- 設定頁籤 -->
            <TabItem Header="⚙️ 設定">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,16,0,0">
                    <StackPanel MaxWidth="800" HorizontalAlignment="Left">
                        
                        <!-- 模組目錄設定卡片 -->
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="模組目錄" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,12"/>
                                <TextBlock Text="選擇 RimWorld 的模組資料夾路徑" Style="{StaticResource LabelTextStyle}"/>
                                
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBox x:Name="FolderPathTextBox" Grid.Column="0"
                                             Style="{StaticResource ModernTextBoxStyle}"
                                             Text="{Binding FolderPath, UpdateSourceTrigger=PropertyChanged}"
                                             TextChanged="FolderPathTextBox_TextChanged"/>
                                    <Button x:Name="BrowseButton" Grid.Column="1" Content="瀏覽..." 
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Margin="8,0,0,0" Click="BrowseButton_Click"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                        
                        <!-- ModsConfig 設定卡片 -->
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="ModsConfig.xml" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,12"/>
                                <TextBlock Text="選擇遊戲的 ModsConfig.xml 檔案以按啟用順序排序模組" Style="{StaticResource LabelTextStyle}"/>
                                
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <Button x:Name="SelectModsConfigButton" Content="選擇檔案" 
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Click="SelectModsConfigButton_Click"/>
                                    <Border Background="#F1F5F9" CornerRadius="6" Padding="12,8" Margin="12,0,0,0"
                                            VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock x:Name="ModsConfigPathText" Text="未選擇" 
                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                            <TextBlock x:Name="ModsConfigStatusText" Text="" Margin="8,0,0,0" 
                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        
                        <!-- 掃描操作卡片 -->
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="掃描模組" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,12"/>
                                <TextBlock Text="掃描模組目錄以載入所有模組資訊" Style="{StaticResource LabelTextStyle}"/>
                                
                                <Button x:Name="ScanButton" Content="🔍 開始掃描" 
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        HorizontalAlignment="Left" Margin="0,12,0,0"
                                        Click="ScanButton_Click"/>
                                
                                <!-- 進度條 -->
                                <StackPanel x:Name="ProgressPanel" Visibility="Collapsed" Margin="0,16,0,0">
                                    <ProgressBar x:Name="ScanProgressBar" Height="8" Minimum="0" Maximum="100" Value="0"
                                                 Style="{StaticResource ModernProgressBarStyle}"/>
                                    <TextBlock x:Name="ProgressTextBlock" Text="準備掃描..." 
                                               Style="{StaticResource LabelTextStyle}" Margin="0,8,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            
            <!-- 模組瀏覽頁籤 -->
            <TabItem Header="📦 模組瀏覽">
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.5*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*" MinWidth="320"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- 模組列表 -->
                    <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="0">
                        <DataGrid x:Name="ModsDataGrid" AutoGenerateColumns="False" 
                                  SelectionMode="Single" IsReadOnly="True"
                                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                                  MouseRightButtonDown="ModsDataGrid_MouseRightButtonDown"
                                  SelectedItem="{Binding SelectedMod}"
                                  Background="Transparent" BorderThickness="0"
                                  RowBackground="White" AlternatingRowBackground="#FAFAFA"
                                  HorizontalGridLinesBrush="{StaticResource BorderBrush}">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F8FAFC"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                    <Setter Property="Padding" Value="12,10"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#EFF6FF"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="模組名稱" Binding="{Binding Name}" Width="*" MinWidth="200"/>
                                <DataGridTextColumn Header="作者" Binding="{Binding Author}" Width="120"/>
                                <DataGridTextColumn Header="版本" Binding="{Binding SupportedVersions}" Width="90">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="{Binding VersionCompatibilityBackground}"/>
                                            <Setter Property="Padding" Value="4,2"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="繁中" Binding="{Binding HasChineseTraditional}" Width="60">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="{Binding HasChineseTraditionalBackground}"/>
                                            <Setter Property="Padding" Value="4,2"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="簡中" Binding="{Binding HasChineseSimplified}" Width="60">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="{Binding HasChineseSimplifiedBackground}"/>
                                            <Setter Property="Padding" Value="4,2"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="補丁" Binding="{Binding HasTranslationPatch}" Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="{Binding HasTranslationPatchBackground}"/>
                                            <Setter Property="Padding" Value="4,2"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="可翻譯" Binding="{Binding CanTranslate}" Width="60">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="{Binding CanTranslateBackground}"/>
                                            <Setter Property="Padding" Value="4,2"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                    
                    <!-- 分隔線 -->
                    <GridSplitter Grid.Column="1" Width="16" Background="Transparent" 
                                  HorizontalAlignment="Center" VerticalAlignment="Stretch"/>
                    
                    <!-- 預覽面板 -->
                    <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel x:Name="PreviewPanel">
                                <TextBlock Text="模組詳情" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,16"/>
                                
                                <!-- 預覽圖片 -->
                                <Border Background="#F8FAFC" CornerRadius="8" Padding="8" Margin="0,0,0,16">
                                    <Image x:Name="PreviewImage" MaxWidth="280" MaxHeight="280" 
                                           Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                
                                <!-- 模組資訊 -->
                                <StackPanel x:Name="ModInfoPanel" Visibility="Collapsed">
                                    <TextBlock x:Name="ModNameText" FontSize="16" FontWeight="SemiBold" 
                                               Foreground="{StaticResource TextPrimaryBrush}" 
                                               TextWrapping="Wrap" Margin="0,0,0,8"/>
                                    
                                    <Border Background="#F8FAFC" CornerRadius="6" Padding="12" Margin="0,0,0,16">
                                        <StackPanel>
                                            <TextBlock x:Name="ModAuthorText" FontSize="13" 
                                                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                                            <TextBlock x:Name="ModPackageIdText" FontSize="12" 
                                                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"
                                                       TextWrapping="Wrap"/>
                                            <TextBlock x:Name="ModVersionText" FontSize="12" 
                                                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                                            <TextBlock x:Name="ModFolderText" FontSize="12" 
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- 翻譯狀態 -->
                                    <TextBlock Text="翻譯狀態" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,12"/>
                                    <Border Background="#F8FAFC" CornerRadius="6" Padding="12" Margin="0,0,0,16">
                                        <StackPanel>
                                            <TextBlock x:Name="ChineseTraditionalText" FontSize="13" Margin="0,0,0,6"/>
                                            <TextBlock x:Name="ChineseSimplifiedText" FontSize="13" Margin="0,0,0,6"/>
                                            <TextBlock x:Name="TranslationPatchText" FontSize="13" Margin="0,0,0,6"/>
                                            <TextBlock x:Name="CanTranslateText" FontSize="13"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- 翻譯補丁列表 -->
                                    <TextBlock x:Name="TranslationPatchesTitle" Text="相關翻譯補丁" 
                                               Style="{StaticResource SubHeaderTextStyle}"
                                               Visibility="Collapsed" Margin="0,0,0,12"/>
                                    <StackPanel x:Name="TranslationPatchesList" Visibility="Collapsed"/>
                                </StackPanel>
                                
                                <!-- 空狀態 -->
                                <StackPanel x:Name="EmptyStateText" HorizontalAlignment="Center" Margin="0,40,0,0">
                                    <TextBlock Text="📋" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                                    <TextBlock Text="選擇一個模組查看詳細資訊" 
                                               FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" 
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>
            
        </TabControl>
        
        <!-- 底部狀態列 -->
        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid Margin="24,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="StatusTextBlock" Text="準備就緒" 
                           VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Grid.Column="1" Text="RimWorld Translation Tool v1.0" 
                           VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
            </Grid>
        </Border>
    </Grid>
</Window>
