<Window x:Class="RimWorldTranslationTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="zh-TW"
        lex:ResxLocalizationProvider.DefaultAssembly="RimWorldTranslationTool"
        lex:ResxLocalizationProvider.DefaultDictionary="Resources"
        Title="{lex:Loc WindowTitle}" Height="750" Width="1300"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 頂部標題區 -->
        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="24,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🎮" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{lex:Loc WindowTitle}" Style="{StaticResource HeaderTextStyle}"/>
                        <TextBlock Text="{lex:Loc Subtitle}" Style="{StaticResource LabelTextStyle}" Margin="0"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- 快速操作區 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🎮" FontSize="24" VerticalAlignment="Center" Margin="0,0,8,0"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 主要內容區 - TabControl -->
        <TabControl Grid.Row="1" Margin="24,16,24,0">
            
            <!-- 設定頁籤 -->
            <TabItem Header="{lex:Loc TabSettings}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,16,0,0">
                    <StackPanel MaxWidth="800" HorizontalAlignment="Left">
                        
                        <!-- RimWorld 路徑設定卡片 -->
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="{lex:Loc RimWorldPathSettings}" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,12"/>
                                <TextBlock Text="{lex:Loc PathSettingsDescription}" Style="{StaticResource LabelTextStyle}"/>
                                
                                <!-- 遊戲路徑 -->
                                <Border Background="#F8FAFC" CornerRadius="8" Padding="16" Margin="0,12,0,0">
                                    <StackPanel>
                                        <TextBlock Text="{lex:Loc GameDirectory}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                        <TextBlock Text="{lex:Loc GameDirectoryDescription}" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,8"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBox x:Name="GamePathTextBox" Grid.Column="0"
                                                     Style="{StaticResource ModernTextBoxStyle}"
                                                     Text="{Binding GamePath, UpdateSourceTrigger=PropertyChanged}"
                                                     TextChanged="GamePathTextBox_TextChanged"/>
                                            <Button x:Name="BrowseGameButton" Grid.Column="1" Content="{lex:Loc Browse}" 
                                                    Style="{StaticResource SecondaryButtonStyle}"
                                                    Margin="8,0,0,0" Click="BrowseGameButton_Click"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                                
                                <!-- 自動檢測的路徑顯示 -->
                                <Border Background="#F0F9FF" CornerRadius="8" Padding="16" Margin="0,12,0,0">
                                    <StackPanel>
                                        <TextBlock Text="{lex:Loc AutoDetectedPaths}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                        <TextBlock Text="{lex:Loc WorkshopPath}" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding WorkshopPath}" FontSize="12" Foreground="#64748B" Margin="0,0,0,8"/>
                                        <TextBlock Text="{lex:Loc ConfigPath}" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding ConfigPath}" FontSize="12" Foreground="#64748B"/>
                                    </StackPanel>
                                </Border>
                                
                                <!-- 自動檢測按鈕 -->
                                <Button Content="{lex:Loc AutoDetectPaths}" 
                                       Style="{StaticResource PrimaryButtonStyle}"
                                       Margin="0,16,0,0" 
                                       Click="AutoDetectPaths_Click"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- ModsConfig.xml 設定卡片 -->
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="{lex:Loc ModsConfigSettings}" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,12"/>
                                <TextBlock Text="{lex:Loc ModsConfigDescription}" Style="{StaticResource LabelTextStyle}"/>
                                
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock x:Name="ModsConfigPathText" Grid.Column="0" 
                                               Text="{lex:Loc NoFileSelected}" VerticalAlignment="Center"
                                               Style="{StaticResource LabelTextStyle}"/>
                                    <TextBlock x:Name="ModsConfigStatusText" Grid.Column="1" 
                                               Text="" VerticalAlignment="Center" Margin="8,0"
                                               Style="{StaticResource LabelTextStyle}"/>
                                    <Button x:Name="SelectModsConfigButton" Grid.Column="2" Content="{lex:Loc SelectFile}" 
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Margin="8,0,0,0" Click="SelectModsConfigButton_Click"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                        
                        <!-- 遊戲版本設定 -->
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="{lex:Loc GameVersion}" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,12"/>
                                <TextBlock Text="{lex:Loc GameVersionDescription}" Style="{StaticResource LabelTextStyle}"/>
                                
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <TextBlock Text="{lex:Loc CurrentVersion}" VerticalAlignment="Center" Margin="0,0,12,0" 
                                               Style="{StaticResource LabelTextStyle}"/>
                                    <ComboBox x:Name="GameVersionComboBox" Width="120" Height="32" 
                                              Style="{StaticResource ModernComboBoxStyle}"
                                              SelectionChanged="GameVersionComboBox_SelectionChanged"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            
            <!-- 模組瀏覽頁籤 -->
            <TabItem Header="{lex:Loc TabModBrowser}">
                <Grid Margin="0,16,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.5*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*" MinWidth="320"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- 掃描控制區 -->
                    <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- 掃描按鈕和狀態 -->
                            <StackPanel Grid.Row="0" Orientation="Horizontal">
                                <Button x:Name="ScanButton" Content="{lex:Loc ScanMods}" 
                                       Style="{StaticResource PrimaryButtonStyle}"
                                       Click="ScanButton_Click"/>
                                <TextBlock x:Name="StatusTextBlock" Text="{lex:Loc ReadyToScan}" 
                                           VerticalAlignment="Center" Margin="12,0,0,0"
                                           Style="{StaticResource LabelTextStyle}"/>
                            </StackPanel>
                            
                            <!-- 進度條 -->
                            <StackPanel x:Name="ProgressPanel" Grid.Row="1" Margin="0,12,0,0"
                                        Visibility="Collapsed">
                                <ProgressBar x:Name="ScanProgressBar" Height="8" 
                                             Background="#E5E7EB" Foreground="#3B82F6"/>
                                <TextBlock x:Name="ProgressTextBlock" Text="{lex:Loc PreparingScan}" 
                                           Margin="0,8,0,0" HorizontalAlignment="Center"
                                           Style="{StaticResource LabelTextStyle}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <!-- 模組列表 -->
                    <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource CardStyle}" Padding="0">
                        <DataGrid x:Name="ModsDataGrid" AutoGenerateColumns="False" 
                                  SelectionMode="Single" IsReadOnly="True"
                                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                                  MouseRightButtonDown="ModsDataGrid_MouseRightButtonDown"
                                  SelectedItem="{Binding SelectedMod}"
                                  Background="Transparent" BorderThickness="0"
                                  RowBackground="White" AlternatingRowBackground="#FAFAFA"
                                  HorizontalGridLinesBrush="{StaticResource BorderBrush}">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F8FAFC"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                    <Setter Property="Padding" Value="12,10"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                            <Setter Property="Opacity" Value="0.5"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#EFF6FF"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{lex:Loc ColumnName}" Binding="{Binding Name}" Width="*" MinWidth="200"/>
                                <DataGridTextColumn Header="{lex:Loc ColumnAuthor}" Binding="{Binding Author}" Width="120"/>
                                <DataGridTextColumn Header="{lex:Loc ColumnVersion}" Binding="{Binding SupportedVersions}" Width="90">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="{Binding VersionCompatibilityBackground}"/>
                                            <Setter Property="Padding" Value="4,2"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="{lex:Loc ColumnTraditionalChinese}" Binding="{Binding HasChineseTraditional}" Width="60">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="{Binding HasChineseTraditionalBackground}"/>
                                            <Setter Property="Padding" Value="4,2"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="{lex:Loc ColumnSimplifiedChinese}" Binding="{Binding HasChineseSimplified}" Width="60">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="{Binding HasChineseSimplifiedBackground}"/>
                                            <Setter Property="Padding" Value="4,2"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="{lex:Loc ColumnPatch}" Binding="{Binding HasTranslationPatch}" Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="{Binding HasTranslationPatchBackground}"/>
                                            <Setter Property="Padding" Value="4,2"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="{lex:Loc ColumnTranslatable}" Binding="{Binding CanTranslate}" Width="60">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="{Binding CanTranslateBackground}"/>
                                            <Setter Property="Padding" Value="4,2"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridCheckBoxColumn Header="{lex:Loc ColumnEnabled}" Binding="{Binding IsEnabled}" Width="50">
                                    <DataGridCheckBoxColumn.ElementStyle>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridCheckBoxColumn.ElementStyle>
                                </DataGridCheckBoxColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                    
                    <!-- 分隔線 -->
                    <GridSplitter Grid.Column="1" Width="16" Background="Transparent" 
                                  HorizontalAlignment="Center" VerticalAlignment="Stretch"/>
                    
                    <!-- 預覽面板 -->
                    <Border Grid.Row="1" Grid.Column="2" Style="{StaticResource CardStyle}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel x:Name="PreviewPanel">
                                <TextBlock Text="{lex:Loc ModDetails}" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,16"/>
                                
                                <!-- 預覽圖片 -->
                                <Border Background="#F8FAFC" CornerRadius="8" Padding="8" Margin="0,0,0,16">
                                    <Image x:Name="PreviewImage" MaxWidth="280" MaxHeight="280" 
                                           Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                
                                <!-- 模組資訊 -->
                                <StackPanel x:Name="ModInfoPanel" Visibility="Collapsed">
                                    <TextBlock x:Name="ModNameText" FontSize="16" FontWeight="SemiBold" 
                                               Foreground="{StaticResource TextPrimaryBrush}" 
                                               TextWrapping="Wrap" Margin="0,0,0,8"/>
                                    
                                    <Border Background="#F8FAFC" CornerRadius="6" Padding="12" Margin="0,0,0,16">
                                        <StackPanel>
                                            <TextBlock x:Name="ModAuthorText" FontSize="13" 
                                                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                                            <TextBlock x:Name="ModPackageIdText" FontSize="12" 
                                                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"
                                                       TextWrapping="Wrap"/>
                                            <TextBlock x:Name="ModVersionText" FontSize="12" 
                                                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                                            <TextBlock x:Name="ModFolderText" FontSize="12" 
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- 翻譯狀態 -->
                                    <TextBlock Text="{lex:Loc TranslationStatus}" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,12"/>
                                    <Border Background="#F8FAFC" CornerRadius="6" Padding="12" Margin="0,0,0,16">
                                        <StackPanel>
                                            <TextBlock x:Name="ChineseTraditionalText" FontSize="13" Margin="0,0,0,6"/>
                                            <TextBlock x:Name="ChineseSimplifiedText" FontSize="13" Margin="0,0,0,6"/>
                                            <TextBlock x:Name="TranslationPatchText" FontSize="13" Margin="0,0,0,6"/>
                                            <TextBlock x:Name="CanTranslateText" FontSize="13"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- 翻譯補丁列表 -->
                                    <TextBlock x:Name="TranslationPatchesTitle" Text="{lex:Loc RelatedTranslationPatches}" 
                                               Style="{StaticResource SubHeaderTextStyle}"
                                               Visibility="Collapsed" Margin="0,0,0,12"/>
                                    <StackPanel x:Name="TranslationPatchesList" Visibility="Collapsed"/>
                                </StackPanel>
                                
                                <!-- 空狀態 -->
                                <StackPanel x:Name="EmptyStateText" HorizontalAlignment="Center" Margin="0,40,0,0">
                                    <TextBlock Text="📋" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                                    <TextBlock Text="{lex:Loc SelectModToViewDetails}" 
                                               FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" 
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>
            
            <!-- 模組管理頁籤 -->
            <TabItem Header="{lex:Loc TabModManager}">
                <Grid Margin="0,16,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- 左側：模組池 -->
                    <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                                <TextBlock Text="{lex:Loc ModPool}" Style="{StaticResource SubHeaderTextStyle}"/>
                                <TextBlock Text="{lex:Loc ModPoolDescription}" Style="{StaticResource LabelTextStyle}" Margin="0,4,0,0"/>
                            </StackPanel>
                            
                            <ListBox x:Name="ModPoolListBox" Grid.Row="1" 
                                     SelectionMode="Extended"
                                     AllowDrop="True"
                                     Drop="ModPoolListBox_Drop"
                                     Background="Transparent" BorderThickness="0">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="White" CornerRadius="6" Padding="12,8" Margin="0,2">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                                                <TextBlock Text="{Binding Author}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                    
                    <!-- 中間：操作區 -->
                    <Grid Grid.Column="1" VerticalAlignment="Center" Margin="16,0">
                        <!-- 移除中間按鈕，改為底部統一操作 -->
                    </Grid>
                    
                    <!-- 右側：啟用列表 -->
                    <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                                <TextBlock Text="{lex:Loc EnabledList}" Style="{StaticResource SubHeaderTextStyle}"/>
                                <TextBlock Text="{lex:Loc EnabledListDescription}" Style="{StaticResource LabelTextStyle}" Margin="0,4,0,0"/>
                            </StackPanel>
                            
                            <ListBox x:Name="EnabledModsListBox" Grid.Row="1"
                                     SelectionMode="Extended"
                                     AllowDrop="True"
                                     Drop="EnabledModsListBox_Drop"
                                     Background="Transparent" BorderThickness="0">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#F0FDF4" CornerRadius="6" Padding="12,8" Margin="0,2">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                                                <TextBlock Text="{Binding Author}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                    
                    <!-- 底部操作區 -->
                    <Border Grid.Row="1" Grid.ColumnSpan="3" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Button Content="{lex:Loc EnableSelected}" Style="{StaticResource PrimaryButtonStyle}" 
                                    Click="MoveToEnabled_Click" Margin="0,0,12,0"/>
                            <Button Content="{lex:Loc DisableSelected}" Style="{StaticResource SecondaryButtonStyle}" 
                                    Click="MoveToPool_Click" Margin="0,0,12,0"/>
                            <Button Content="{lex:Loc Refresh}" Style="{StaticResource SecondaryButtonStyle}" 
                                    Click="RefreshModLists_Click" Margin="0,0,12,0"/>
                            <Button Content="{lex:Loc Diagnose}" Style="{StaticResource SecondaryButtonStyle}" 
                                    Click="DiagnoseModsConfig_Click" Margin="0,0,12,0"/>
                            <Button Content="{lex:Loc SaveConfig}" Style="{StaticResource PrimaryButtonStyle}" 
                                    Click="SaveModsConfig_Click"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
            
        </TabControl>
        
        <!-- 底部狀態列 -->
        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid Margin="24,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="1" Text="{lex:Loc AppVersion}" 
                           VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
            </Grid>
        </Border>
    </Grid>
</Window>
